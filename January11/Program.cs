// Сортировка выбором 

using static Sorting;
using static Infastructure;

// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

10.CreateArray(min: 5, max: 20)
  .Show("|")
  .SortSelection()
  .Show("-");